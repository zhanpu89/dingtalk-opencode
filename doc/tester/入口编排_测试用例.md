# 入口编排 测试用例

---
文档编号：TC-20260615-001
版本：v1.0.0
状态：🟡 草稿
创建日期：2026-06-15
最后更新：2026-06-15
作者：AI-Tester
关联文档：
  - 详设文档：docs/design.md（§2.1 入口编排）
---

## 1. 测试范围

- **被测模块**：入口编排（`src/index.ts`——消息接收、解析、鉴权、流程编排）
- **被测接口**：无独立对外 API（内部模块协作：`MessageQueue`、`OpenCodeClient`、`DingTalkClient`、`SessionStore`、`Watchdog`）
- **用例总数**：13 条（单元测试 9 + 集成测试 4）
- **关联业务规则**：BR-ENTRY-001~BR-ENTRY-013
- **关联代码评审报告**：无

---

## 2. 全局前置条件

1. Mock 外部依赖：`OpenCodeClient`、`DingTalkClient`、`SessionStore`、`MessageQueue`、`Watchdog`
2. 环境变量已通过 `loadConfig` 加载（可 Mock）
3. 所有测试不依赖真实钉钉/OpenCode 服务

---

## 3. 单元测试用例（UNIT）

### 业务规则覆盖用例

| 用例 ID | 用例标题 | 优先级 | 关联规则 | 前置条件 | 测试步骤 | 预期结果 | 备注 |
|--------|---------|-------|---------|---------|---------|---------|-----|
| TC-ENTRY-UNIT-001 | 正常消息处理——收到并回复 | P0 | BR-ENTRY-001 | Mock 外部依赖；传入合法 RobotTextMessage | 1. 调用 `handleRobotMessage`，msgtype=text，内容含 `@botName 修复bug` 2. 验证 queue.enqueue 被调用，参数 sessionKey 正确 3. 验证 enqueue 的回调中依次调用了 sessions.get / opencode.sendMessage / watchdog.start 等 | enqueue 被调用；回调内按序执行各步骤 | |
| TC-ENTRY-UNIT-002 | 空消息（仅 @提及）回复引导提示 | P0 | BR-ENTRY-002 | Mock DingTalkClient；传入内容仅为 `@botName` 的消息 | 1. 调用 `handleRobotMessage`，文本为 `@botName` 2. 验证 `dingtalk.sendTextMessage` 被调用且内容含引导提示 | 回复"你好 xxx 👋..."引导提示，不进入 queue.enqueue | |
| TC-ENTRY-UNIT-003 | 非 text 类型消息忽略 | P1 | BR-ENTRY-003 | Mock Logger；传入 msgtype=image 的消息 | 1. 调用 `handleRobotMessage`，msgtype="image" 2. 验证 log.debug 输出 "ignored non-text message" | 不回复，不进入 queue.enqueue | |
| TC-ENTRY-UNIT-004 | 非法 JSON 日志警告不回复 | P1 | BR-ENTRY-004 | Mock Logger；传入非 JSON 字符串 | 1. 调用 `handleRobotMessage("not json")` 2. 验证 log.warn 被调用 | 日志警告，不回复 | |
| TC-ENTRY-UNIT-005 | session 首次创建调用 createSession | P1 | BR-ENTRY-005 | Mock: `sessions.get` 返回 undefined；`opencode.createSession` 返回 `{id: "new-id"}` | 1. 触发正常消息处理流程 2. 验证 `sessions.get` 被调用 3. 验证 `opencode.createSession` 被调用并传入标题 `钉钉-${senderNick}` | sessions.get 返回 undefined → createSession 被调用 → sessions.set 被调用 | |
| TC-ENTRY-UNIT-006 | session 已存在复用现有 sessionId | P1 | BR-ENTRY-006 | Mock: `sessions.get` 返回 `"existing-id"` | 1. 触发正常消息处理流程 2. 验证 createSession 未被调用 | 不走 createSession，直接使用返回的 sessionId | |
| TC-ENTRY-UNIT-007 | DingTalk 回复失败日志记录 | P1 | BR-ENTRY-007 | Mock: `dingtalk.sendMessage` throw 异常；`opencode.sendMessage` 正常返回 | 1. 触发正常消息处理流程 2. 验证 catch 块中 log.error 被调用 3. 验证 dingtalk.sendTextMessage 发送失败通知 | 日志记录错误；用户收到"任务已完成但结果发送失败"通知 | |
| TC-ENTRY-UNIT-008 | 超时 (AbortError) 提示"任务超时" | P0 | BR-ENTRY-008 | Mock: `opencode.sendMessage` 抛出 Error 且 name="AbortError" | 1. 触发消息处理流程 2. AbortError 被 catch 3. 验证 `sendProcessingError` 被调用且 `isTimeout` 为 true | 用户收到"⚠️ 任务超时"提示 | |
| TC-ENTRY-UNIT-009 | 其他错误提示处理失败 + 错误原因 | P1 | BR-ENTRY-009 | Mock: `opencode.sendMessage` 抛出普通 Error | 1. 触发消息处理流程 2. 验证 `sendProcessingError` 被调用，isTimeout=false | 用户收到"❌ 处理失败"提示，含错误原因 | |

### 分支覆盖用例

| 用例 ID | 用例标题 | 优先级 | 对应分支 | 前置条件 | 测试步骤 | 预期结果 | 备注 |
|--------|---------|-------|---------|---------|---------|---------|-----|
| TC-ENTRY-UNIT-010 | buildReplyMessage——有摘要无文件 | P2 | `summary` 非空 + `changedFiles` 为空 | 传入 summary="摘要内容"，changedFiles=[] | 1. 调用 `buildReplyMessage` | 结果含摘要，不含文件列表 | |
| TC-ENTRY-UNIT-011 | buildReplyMessage——有文件无摘要 | P2 | `summary` 为 `"(无文本回复)"` + `changedFiles` 非空 | 传入 summary="(无文本回复)"，changedFiles=["a.ts"] | 1. 调用 `buildReplyMessage` | 结果不含摘要，含文件列表 | |
| TC-ENTRY-UNIT-012 | buildReplyMessage——有 shareUrl | P2 | `shareUrl` 非 null | 传入 shareUrl="https://..." | 1. 调用 `buildReplyMessage` | 结果含"查看完整对话"链接 | |
| TC-ENTRY-UNIT-013 | buildReplyMessage——无 shareUrl | P2 | `shareUrl` 为 null | 传入 shareUrl=null | 1. 调用 `buildReplyMessage` | 结果含"会话ID"文本 | |

---

## 4. 集成测试用例（INTG）

### 业务流程用例（跨模块链路）

| 用例 ID | 用例标题 | 优先级 | 流程步骤 | 预期最终状态 | 备注 |
|--------|---------|-------|---------|------------|-----|
| TC-ENTRY-INTG-001 | 正常消息处理端到端流程 | P0 | 1. 传入合法 RobotTextMessage 2. 发送"已收到消息..." 3. 创建/复用 session 4. watchdog.start() 5. opencode.sendMessage() 6. watchdog.stop() 7. 构建回复并发送 | 完整回复发送到钉钉，heartbeat 清除 | |
| TC-ENTRY-INTG-002 | 看门狗 restart 触发——重试后成功 | P0 | 1. 正常流程启动 2. opencode.sendMessage 抛出 AbortError 3. watchdog.state === "restart" 4. retries < 1 → 创建新 session 重试 5. 重试成功 | 重试 1 次后成功回复，retries=1 | |
| TC-ENTRY-INTG-003 | 看门狗 server_down 触发 | P0 | 1. 正常流程启动 2. watchdog.state === "server_down" 3. 进入 catch 块 | 用户收到"❌ 处理失败——OpenCode 服务不可用" | |
| TC-ENTRY-INTG-004 | 消息队列串行——同一 sessionKey | P1 | 1. 同一 sessionKey 入队 2 个任务 2. 第 1 个任务执行中 3. 第 1 个完成后第 2 个开始 | 第 2 个在第 1 个完成后执行 | |

---

## 5. 安全测试用例（SEC）

本模块无安全测试用例（模块为非外部 API 入口，仅处理钉钉回调消息，不涉及认证/授权/注入）

---

## 6. 性能测试用例（PERF）

本模块无性能测试用例（模块为编排层，非性能瓶颈点）

---

## 7. 前端测试用例（FE）

本模块无前端测试用例（纯后端项目）

---

## 8. 用例统计

| 类型 | 数量 | 优先级分布 |
|-----|-----|---------|
| 单元测试（UNIT） | 13 | P0: 4 / P1: 6 / P2: 3 |
| 集成测试（INTG） | 4 | P0: 3 / P1: 1 / P2: 0 |
| 安全测试（SEC） | 0 | - |
| 性能测试（PERF） | 0 | - |
| 前端测试（FE） | 0 | - |
| **合计** | **17** | P0: 7 / P1: 7 / P2: 3 |

其中 `[评审专项]` 用例：0 条

---

## 变更记录

| 版本 | 日期 | 作者 | 变更说明 |
|-----|------|-----|---------|
| v1.0.0 | 2026-06-15 | AI-Tester | 初始版本，首次生成 |
